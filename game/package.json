{
  "name": "@ichi/game",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsgo -p tsconfig.json",
    "watch": "tsgo -w -p tsconfig.json",
    "typecheck": "tsgo --noEmit -p tsconfig.json",
    "test": "npm run build && node --test ../dist/game/*.test.js"
  }
}
